# 스타일과 라우팅

## Tailwind CSS

### 설치
#### https://tailwindcss.com

#### 1. Create your project
```
npm create vite@latest my-project
cd my-project
```

#### 2. Install Tailwind CSS
```
npm install tailwindcss @tailwindcss/vite
```

#### 3. Configure the Vite plugin

```
import { defineConfig } from 'vite'
import tailwindcss from '@tailwindcss/vite'
export default defineConfig({
  plugins: [
    tailwindcss(),
  ],
})
```
#### vite.config.ts 추가 이렇케 추가
```
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import tailwindcss from "@tailwindcss/vite";

// https://vite.dev/config/
export default defineConfig({
  plugins: [react(), tailwindcss()],
});
```

#### 4. Import Tailwind CSS
index.css 추가
```
@import "tailwindcss";
```

#### 5. Start your build process
```
npm run dev
```

#### 6. Start using Tailwind in your HTML
```
import { useState } from "react";
import reactLogo from "./assets/react.svg";
import viteLogo from "/vite.svg";
import "./App.css";

function App() {
  const [count, setCount] = useState(0);

  return <div className="font-bold underline">Hello World</div>;
}****

export default App;
```

#### Tailwind CSS IntelliSense 설치
- 클래스 자동완성 (Auto-completion)
- 실시간 색상 미리보기
- 오타 검사 (Linting)
![](https://github.com/dididiri1/TIL/blob/main/React/v2/images/02_01.png?raw=true)

#### prettier 설치
- 코드 자동 정렬 도구 Prettier + TailwindCSS 정렬 플러그인
```
npm i -D prettier prettier-plugin-tailwindcss
```

#### .prettier 파일 생성 추가
```
{
  "plugins": ["prettier-plugin-tailwindcss"]
}
```
- 레이아웃 기준으로 정렬됨
```
import { useState } from "react";
import "./App.css";

function App() {
  const [count, setCount] = useState(0);

  return <div className="text-2xl font-bold underline">Hello World</div>;
}

export default App;
```

## Tailwind CSS 핵심정리
```
import { useState } from "react";
import reactLogo from "./assets/react.svg";
import viteLogo from "/vite.svg";
import "./App.css";

function App() {
  const [count, setCount] = useState(0);

  return (
    <div>
      {/* 1. 타이포그래프 */}
      <div className="text-xs text-red-500">text-xs</div>
      <div className="text-sm text-[rgb(100,30,200)]">text-sm</div>
      <div className="text-lg font-bold">text-lg</div>
      <div className="text-xl font-extrabold">text-xl</div>
      <div className="text-2xl font-black">text-2xl</div>
      <div className="text-[13px]">text-13px</div>

      {/* 2. 백그라운드 컬러 */}
      <div className="bg-amber-500">amber-50</div>

      {/* 3. 사이즈 */}
      {/* spacing(4px) w-20 = 4px * 2px = 40px 적용됨 */}
      <div className="w-20 bg-blue-500">box</div>
      <div className="w-[22px]">box</div>
      <div className="w-full bg-blue-500">box</div>

      <div className="h-20 bg-blue-500">box</div>

      {/* 4. 여백 */}
      <div className="h-50 w-50 bg-red-400 pt-5 pr-5 pb-5 pl-5">
        <div className="h-full w-full bg-blue-400">box</div>
      </div>

      <div className="mx-5 my-5 h-50 w-50 bg-red-400 px-5 py-5">
        <div className="h-full w-full bg-blue-400">box</div>
      </div>

      {/* 5.  */}
      <div></div>
    </div>
  );
}

export default App;
```

## Tailwind CSS 핵심정리 2
```
import { useState } from "react";
import reactLogo from "./assets/react.svg";
import viteLogo from "/vite.svg";
import "./App.css";

function App() {
  const [count, setCount] = useState(0);

  return (
    <div>
      
      ...
      
      {/* 6. 플렉스 컨테이너 */}
      {/* flex-1 요소만 최대 넓이 */}
      <div className="flex items-start justify-evenly">
        <div className="h-10 w-10 border">1</div>
        <div className="h-20 w-10 flex-1 border">2</div>
        <div className="h-30 w-10 border">3</div>
        <div className="h-40 w-10 border">4</div>
      </div>
    </div>
  );
}

export default App;
```

## Shadnc/ui
- Tailwind 기반 UI 컴포넌트를 프로젝트 안에 직접 복사해서 쓰는 방식의 컴포넌트 컬렉션

### Shadnc/ui 설치
Shadcn/ui 홈페이지 주소
https://ui.shadcn.com/

![](https://github.com/dididiri1/TIL/blob/main/React/v2/images/02_02.png?raw=true)

### 1. React 프로젝트 생성 (Vite)
```
npm create vite@latest
```

### 2. Tailwind CSS 설치
```
npm install tailwindcss @tailwindcss/vite
```

### 3. src/index.css 수정
src/index.css 전체 삭제 후 아래로 대체:
```
@import "tailwindcss";
```

### 4. tsconfig.json 수정
```
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

### 5. tsconfig.app.json 수정
```
{
  "compilerOptions": {
    // ...
    "baseUrl": ".",
    "paths": {
      "@/*": [
        "./src/*"
      ]
    }
    // ...
  }
}
```

### 6. vite.config.ts 수정
- Node 경로 타입 설치:
```
npm install -D @types/node
```
- vite.config.ts 전체 또는 plugins 아래 수정:
```
import path from "path"
import tailwindcss from "@tailwindcss/vite"
import react from "@vitejs/plugin-react"
import { defineConfig } from "vite"

// https://vite.dev/config/
export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
});
```

### 7. shadcn/ui 초기 설정
- CLI 실행:
```
npx shadcn@latest init
```

### 8. 컴포넌트 추가하기
- 예: Button 컴포넌트 설치
```
npx shadcn@latest add button
```
- src/App.tsx에서 바로 사용:
```tsx
import { Button } from "@/components/ui/button"

function App() {
  return (
    <div className="flex min-h-svh flex-col items-center justify-center">
      <Button>버튼!</Button>
    </div>
  )
}

export default App
```

```
import { useState } from "react";
import reactLogo from "./assets/react.svg";
import viteLogo from "/vite.svg";
import "./App.css";
import { Button } from "./components/ui/button";
import { cn } from "./lib/utils";

function App() {
  const [count, setCount] = useState(0);
  const isActive = true;

  return (
    <div>
      <Button>버튼!</Button>
      <div
        className={cn(
          "w-10 text-sm",
          isActive ? "text-green-500" : "text-red-500",
        )}
      >
        isActive
      </div>
      <div className="text-primary">Primary</div>
      <div className="text-muted">Muted</div>
      <div className="text-destructive">Destructive</div>
    </div>
  );
}

export default App;
```

```
const a: number = 1;
```